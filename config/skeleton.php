<?php

/*
 * access values here like: if (config('skeleton.BeAwesome')) { print 'Rock On!';}
 * Make your own overrides from these defaults by editing /config/skeleton.php
 *  (if that file is not there, make it, or run 'php artisan
 */
return [
    // 'BeAwesome'=>true
];
