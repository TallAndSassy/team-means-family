<?php

/*
 * access values here like: if (config('team-means-family.BeAwesome')) { print 'Rock On!';}
 * Make your own overrides from these defaults by editing /config/team-means-family.php
 *  (if that file is not there, make it, or run 'php artisan
 */
return [
    // 'BeAwesome'=>true
];
